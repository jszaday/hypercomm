mainmodule tester {
    mainchare main {
        entry main(CkArgMsg*);
    };

    array [Max] locality : hypercomm::locality_base_ {
        entry locality(int);
    };

    initproc void enroll_polymorphs(void);
}
