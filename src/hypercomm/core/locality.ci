module locality {
    array [Max] locality_base_ {
        entry locality_base_(void);
        entry void demux(hypercomm_msg *msg);
        entry void execute(CkMessage*);
    };

    group locality_map_ : CkArrayMap {
        entry locality_map_(const bool& node_level);
    };
}
