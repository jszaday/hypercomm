module locality {
    array [Max] locality_base_ {
        entry locality_base_(void);
        entry [threaded] void demux(hypercomm_msg *msg);
        entry [threaded] void execute(CkMessage*);
    };
}
