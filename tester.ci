mainmodule tester {
    mainchare tester {
        entry tester(CkArgMsg*);
    };

    chare other {
        entry other(const placeholder&,const placeholder&);
    };

    initproc void setup_tester(void);
}
